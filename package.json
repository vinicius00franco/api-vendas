{
  "name": "meuapp",
  "version": "1.0.0",
  "main": "src/server.ts",
  "type": "module",
  "license": "MIT",
  "dependencies": {
    "@types/express": "^5.0.3",
    "express": "^5.1.0",
    "reflect-metadata": "^0.2.0",
    "pg": "^8.13.0",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
  "typeorm": "node --loader ts-node/esm --experimental-specifier-resolution=node ./node_modules/typeorm/cli.js -d src/shared/database/data-source.ts",
    "migration:run": "npm run typeorm migration:run",
    "migration:revert": "npm run typeorm migration:revert"
  }
 
}
